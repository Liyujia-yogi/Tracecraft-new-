function safeJson(value) {
  return JSON.stringify(value).replace(/[<>&]/g, (character) => ({ '<': '\\u003c', '>': '\\u003e', '&': '\\u0026' })[character])
}

export function renderAnalysisHtml(data) {
  const title = String(data.meta?.title || data.meta?.product || '设计师需求解析')
  const pageFlow = data.pageFlow || { lanes: [], pages: [], edges: [], unknownEdges: [], coverage: [] }
  return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="analysis-render-version" content="diagram-router-v8">
<title>${title.replace(/[<>&"]/g, '')}</title>
<style>
.svg{display:block!important;width:auto!important;min-width:100%!important;max-width:none!important;height:auto!important}
.node-title{font-size:13px!important}
.node-sub{fill:#667085!important;font-size:11px!important}
.edge{stroke-linejoin:round;stroke-linecap:round}
.edge-label{fill:#526079!important;font-size:11px!important;font-weight:500;text-anchor:middle}
.edge-label-box{fill:#fff!important;fill-opacity:.96;stroke:#cfd8e6!important;stroke-width:1}
.edge-hit{fill:none;stroke:rgba(36,99,184,.001);stroke-width:16;pointer-events:stroke;cursor:pointer}
.edge-hit:focus{outline:none}.edge-hit:focus-visible{stroke:rgba(70,87,200,.14)}
.edge.is-selected{stroke-width:3.4;filter:drop-shadow(0 1px 2px rgba(36,99,184,.38))}
.edge-hit.is-selected{stroke:rgba(36,99,184,.12)}
.edge-label-group{cursor:pointer}
.edge-label-group.is-selected .edge-label-box{fill:#eef4ff!important;stroke:#2463b8!important;stroke-width:1.5}
.edge-label-group.is-selected .edge-label{fill:#174ea6!important;font-weight:700}
:root{--ink:#172033;--muted:#667085;--brand:#4657c8;--blue:#2463b8;--teal:#087ea4;--line:#d9dfeb;--soft:#f4f6fa;--head:#eef2f7;--warn:#d97706;--risk:#b54747;--success:#287a4b}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--soft);color:var(--ink);font:14px/1.6 Inter,Segoe UI,PingFang SC,Microsoft YaHei,sans-serif}.shell{max-width:1680px;margin:auto;background:#fff;min-height:100vh;padding:38px}.hero{padding:4px 0 22px;border-bottom:1px solid var(--line)}.hero h1{font-size:30px;line-height:1.25;margin:0 0 8px}.hero p{margin:0;color:var(--muted)}.tabs{position:sticky;top:0;z-index:20;display:flex;gap:28px;overflow:auto;background:#fff;border-bottom:1px solid var(--line)}button{font:inherit}.tab,.subtab,.tool{border:0;background:transparent;color:var(--muted);cursor:pointer}.tab{padding:15px 3px 12px;white-space:nowrap;border-bottom:3px solid transparent}.tab:hover,.tab:focus-visible,.tab.on{color:var(--brand)}.tab.on{border-color:var(--brand);font-weight:700}.tab:focus-visible,.subtab:focus-visible,.tool:focus-visible{outline:2px solid #9aa7ea;outline-offset:2px}.panel[hidden]{display:none}.section{padding:28px 0;border-bottom:1px solid var(--line)}.section h2{font-size:21px;margin:0 0 14px}.section h3{font-size:16px;margin:22px 0 9px}.section h4{font-size:14px;margin:16px 0 8px}.summary{max-width:1120px;margin:14px 0;color:#344054}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.metric{border:1px solid var(--line);padding:14px 16px;min-height:84px}.metric small{display:block;color:var(--muted)}.metric strong{display:block;margin-top:4px;color:var(--brand);font-size:24px}.facts{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}.fact{border-left:3px solid #aab5c8;background:#f8fafc;padding:10px 13px}.fact b{display:block;margin-bottom:3px}.blockers{margin-top:18px;border-left:3px solid var(--warn);background:#fffaf0;padding:6px 14px 14px}.empty{color:var(--muted);padding:12px 0}.diagram{position:relative;overflow:auto;border:1px solid var(--line);background:#fff}.diagram-toolbar{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:8px 10px;border-bottom:1px solid var(--line);background:#f8fafc}.diagram-toolbar span{color:var(--muted);font-size:12px}.tool{padding:5px 9px;border:1px solid var(--line);background:#fff;color:#344054}.svg{display:block;min-width:920px;width:100%;height:auto}.phase{fill:#eaf0f8;stroke:var(--line)}.lane{fill:#eef3f8;stroke:var(--line)}.cell{fill:#fff;stroke:var(--line)}.node{fill:#fff;stroke:#7592b6;stroke-width:1.2}.node.inference{stroke:var(--warn);stroke-dasharray:5 3}.node.exception,.node.rollback{stroke:var(--risk);stroke-dasharray:5 3}.mid{text-anchor:middle}.hd,.lane-text,.node-title{fill:var(--ink);font-weight:700}.node-sub,.edge-label{fill:var(--muted);font-size:10px}.edge{fill:none;stroke:var(--blue);stroke-width:1.8}.edge.branch{stroke:#6c5ce7}.edge.exception,.edge.rollback{stroke:var(--risk);stroke-dasharray:5 3}.edge.unknown{stroke:var(--warn);stroke-dasharray:5 3}.legend{display:flex;flex-wrap:wrap;gap:14px;padding:8px 11px;color:var(--muted);font-size:12px}.legend i{display:inline-block;width:24px;height:0;margin-right:5px;border-top:2px solid var(--blue);vertical-align:middle}.legend .branch{border-color:#6c5ce7}.legend .risk{border-color:var(--risk);border-top-style:dashed}.legend .unknown{border-color:var(--warn);border-top-style:dashed}.table-wrap{overflow:auto;border:1px solid var(--line);background:#fff}table{width:100%;border-collapse:collapse;min-width:720px}th{position:sticky;top:48px;z-index:2;background:var(--head);text-align:left;font-weight:700}th,td{padding:9px 10px;border-bottom:1px solid #e5e9f0;vertical-align:top;word-break:break-word}tbody tr:last-child td{border-bottom:0}.sources{display:block;max-width:100%;overflow-x:auto;overflow-y:hidden;white-space:nowrap;padding-bottom:2px}.source{display:inline-block;margin:2px 4px 2px 0;padding:1px 6px;border:1px solid #cbd3df;color:#526079;font-size:11px;line-height:1.5}.status{display:inline-block;padding:1px 7px;border:1px solid var(--line);font-size:11px}.status-risk{border-color:#e0abab;color:var(--risk)}.status-warn{border-color:#e7c48b;color:#9a5b00}.status-ok{border-color:#a8d1b7;color:var(--success)}.subtabs{display:flex;gap:7px;overflow:auto;padding:8px;background:var(--soft);margin-bottom:14px}.subtab{padding:8px 10px;white-space:nowrap;border:1px solid transparent}.subtab.on{background:#fff;color:var(--brand);border-color:var(--line);font-weight:700}.subtab.inference{border-style:dashed;border-color:#d8a34f}.detail{border:1px solid var(--line);padding:18px}.detail-header{display:flex;justify-content:space-between;gap:16px;align-items:flex-start}.detail-header h3{margin:0}.meta{color:var(--muted);margin:4px 0 0}.detail-block{padding:8px 0 16px;border-bottom:1px solid #e5e9f0}.detail-block:last-child{border-bottom:0;padding-bottom:0}.context-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.list{margin:6px 0;padding-left:20px}.list li+li{margin-top:5px}.step-list{counter-reset:step;list-style:none;padding:0;margin:8px 0}.step-list li{position:relative;padding:8px 10px 8px 38px;border-left:2px solid #c9d3e2}.step-list li:before{counter-increment:step;content:counter(step);position:absolute;left:8px;top:8px;width:20px;height:20px;text-align:center;border:1px solid #9aa9bf;color:var(--brand);font-size:11px}.review-card{padding:18px 0;border-top:1px solid var(--line)}.review-card:first-of-type{border-top:0}.evidence-link{color:var(--brand);word-break:break-all}.evidence-link:hover{text-decoration:none}.back-top{position:fixed;right:20px;bottom:20px;padding:8px 12px;border:1px solid var(--line);background:#fff;color:var(--ink);cursor:pointer}.print-only{display:none}@media(max-width:1100px){.shell{padding:28px}.metrics{grid-template-columns:repeat(2,1fr)}.facts,.context-grid{grid-template-columns:1fr}}@media(max-width:700px){.shell{padding:14px}.hero h1{font-size:25px}.metrics{grid-template-columns:1fr}.tabs{gap:20px}.detail{padding:14px}.back-top{right:10px;bottom:10px}}@media print{body{background:#fff}.shell{padding:0;max-width:none}.tabs,.back-top,.diagram-toolbar{display:none}.panel[hidden]{display:block}.section{break-inside:avoid}.print-only{display:block}}
.wide-table table{min-width:1420px}.evidence-table table{min-width:980px}.table-wrap{overflow-x:auto;overflow-y:visible;position:relative}th{position:static!important;top:auto!important;z-index:auto!important;white-space:nowrap}.table-complete table{table-layout:fixed}.table-complete td{overflow-wrap:anywhere;word-break:break-word}.outcome-feedback table{min-width:1040px}.outcome-review table,.review-table table{min-width:1280px}.terms-table table{min-width:1080px}.blocker-table table{min-width:1180px}.field-table table{min-width:1180px}.type-badge{display:inline-block;min-width:64px;padding:3px 8px;border:1px solid;border-radius:2px;font-size:12px;font-weight:700;text-align:center}.type-design{color:#3656b3;background:#eef2ff;border-color:#b9c5ef}.type-risk{color:#a63f32;background:#fff1ee;border-color:#e7b6ae}.type-protection{color:#247047;background:#eef9f2;border-color:#b8dcc5}.row-risk td:first-child{border-left:3px solid #c55243}.row-design td:first-child{border-left:3px solid #5269c7}.row-protection td:first-child{border-left:3px solid #3d8a5e}.review-card{margin:10px 0;padding:0;border:1px solid var(--line);background:#fff}.review-card summary{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:9px;padding:13px 15px;background:#f7f9fc;cursor:pointer;font-weight:700;list-style:none}.review-card summary::-webkit-details-marker{display:none}.review-card summary:before{content:'▶';color:#526079;font-size:11px}.review-card[open] summary:before{content:'▼'}.review-card summary small{color:var(--muted);font-weight:400}.review-card-body{padding:12px}.node-title{font-size:12px}.node-sub{font-size:10px}.edge-label-box{fill:#fff;stroke:#d9dfeb}.edge-label{text-anchor:middle}.diagram-note{color:var(--muted);font-size:12px}
</style></head><body><div class="shell"><header class="hero"><h1 id="title"></h1><p id="subtitle"></p></header><nav id="tabs" class="tabs" aria-label="分析模块"></nav><main id="app"></main></div><button id="back-top" class="back-top" type="button">返回顶部</button>
<script id="analysis-data" type="application/json">${safeJson(data)}</script>
<script id="page-flow-schema" type="application/json">${safeJson(pageFlow)}</script>
<script>
(()=>{'use strict';
setTimeout(()=>{swimlane=swimlaneV2;pageFlow=pageFlowV2;const active=document.getElementById('tab-requirement-analysis');if(active?.classList.contains('on'))active.click()},0);
function flowPointKey(point){return Math.round(point[0]*10)/10+'|'+Math.round(point[1]*10)/10}
function flowRect(box,padding=0){return{x:box.x-padding,y:box.y-padding,x2:box.x+box.w+padding,y2:box.y+box.h+padding}}
function flowPointBlocked(point,obstacles){return obstacles.some(box=>point[0]>box.x&&point[0]<box.x2&&point[1]>box.y&&point[1]<box.y2)}
function flowSegmentBlocked(a,b,obstacles){if(a[0]===b[0])return obstacles.some(box=>a[0]>box.x&&a[0]<box.x2&&Math.max(Math.min(a[1],b[1]),box.y)<Math.min(Math.max(a[1],b[1]),box.y2));if(a[1]===b[1])return obstacles.some(box=>a[1]>box.y&&a[1]<box.y2&&Math.max(Math.min(a[0],b[0]),box.x)<Math.min(Math.max(a[0],b[0]),box.x2));return true}
function flowSegmentPenalty(a,b,used){let penalty=0;for(const segment of used){const c=segment[0],d=segment[1];if(a[0]===b[0]&&c[0]===d[0]&&a[0]===c[0]){const overlap=Math.min(Math.max(a[1],b[1]),Math.max(c[1],d[1]))-Math.max(Math.min(a[1],b[1]),Math.min(c[1],d[1]));if(overlap>0)penalty+=80+overlap*1.5}else if(a[1]===b[1]&&c[1]===d[1]&&a[1]===c[1]){const overlap=Math.min(Math.max(a[0],b[0]),Math.max(c[0],d[0]))-Math.max(Math.min(a[0],b[0]),Math.min(c[0],d[0]));if(overlap>0)penalty+=80+overlap*1.5}else if(a[0]===b[0]&&c[1]===d[1]&&a[0]>Math.min(c[0],d[0])&&a[0]<Math.max(c[0],d[0])&&c[1]>Math.min(a[1],b[1])&&c[1]<Math.max(a[1],b[1]))penalty+=42;else if(a[1]===b[1]&&c[0]===d[0]&&c[0]>Math.min(a[0],b[0])&&c[0]<Math.max(a[0],b[0])&&a[1]>Math.min(c[1],d[1])&&a[1]<Math.max(c[1],d[1]))penalty+=42}return penalty}
function flowPort(box,side,clearance=15,ratio=.5){const centers={left:[box.x,box.y+box.h*ratio],right:[box.x+box.w,box.y+box.h*ratio],top:[box.x+box.w*ratio,box.y],bottom:[box.x+box.w*ratio,box.y+box.h]},vectors={left:[-1,0],right:[1,0],top:[0,-1],bottom:[0,1]},point=centers[side],vector=vectors[side];return{side,ratio,point,lead:[point[0]+vector[0]*clearance,point[1]+vector[1]*clearance],dir:vector[0]===0?1:0}}
function flowPortBias(side,box,target){const dx=target.x+target.w/2-(box.x+box.w/2),dy=target.y+target.h/2-(box.y+box.h/2),preferred=Math.abs(dx)>=Math.abs(dy)?(dx>=0?'right':'left'):(dy>=0?'bottom':'top');if(side===preferred)return 0;if((side==='left'||side==='right')===(preferred==='left'||preferred==='right'))return 70;return 24}
function flowSimplify(points){const output=[];for(const point of points){const previous=output.at(-1);if(previous&&previous[0]===point[0]&&previous[1]===point[1])continue;const before=output.at(-2);if(before&&previous&&(before[0]===previous[0]&&previous[0]===point[0]||before[1]===previous[1]&&previous[1]===point[1]))output[output.length-1]=point;else output.push(point)}return output}
function flowRoute(from,to,allBoxes,used,width,height){const clearance=15,padding=9,portRatios=[.2,.35,.5,.65,.8],sourcePorts=['left','right','top','bottom'].flatMap(side=>portRatios.map(ratio=>flowPort(from,side,clearance,ratio))),targetPorts=['left','right','top','bottom'].flatMap(side=>portRatios.map(ratio=>flowPort(to,side,clearance,ratio))),obstacles=allBoxes.map(box=>flowRect(box,padding)),xs=new Set([8,width-8]),ys=new Set([8,height-8]);for(const box of obstacles){[box.x-12,box.x,box.x2,box.x2+12].forEach(value=>xs.add(Math.max(6,Math.min(width-6,value))));[box.y-12,box.y,box.y2,box.y2+12].forEach(value=>ys.add(Math.max(6,Math.min(height-6,value))))}for(const port of [...sourcePorts,...targetPorts]){xs.add(port.lead[0]);ys.add(port.lead[1])}const xValues=[...xs].sort((a,b)=>a-b),yValues=[...ys].sort((a,b)=>a-b),points=[],indexByKey=new Map();for(const y of yValues)for(const x of xValues){const point=[x,y];if(flowPointBlocked(point,obstacles))continue;indexByKey.set(flowPointKey(point),points.length);points.push(point)}const neighbors=Array.from({length:points.length},()=>[]),rows=new Map(),columns=new Map();points.forEach((point,index)=>{(rows.get(point[1])||rows.set(point[1],[]).get(point[1])).push(index);(columns.get(point[0])||columns.set(point[0],[]).get(point[0])).push(index)});const connect=(group,axis)=>{group.sort((a,b)=>points[a][axis]-points[b][axis]);for(let i=1;i<group.length;i++){const a=group[i-1],b=group[i],pa=points[a],pb=points[b];if(flowSegmentBlocked(pa,pb,obstacles))continue;const distance=Math.abs(pa[0]-pb[0])+Math.abs(pa[1]-pb[1]),dir=pa[0]===pb[0]?1:0;neighbors[a].push([b,distance,dir]);neighbors[b].push([a,distance,dir])}};rows.forEach(group=>connect(group,0));columns.forEach(group=>connect(group,1));const targetByNode=new Map();targetPorts.forEach(port=>{const index=indexByKey.get(flowPointKey(port.lead));if(index!==undefined)(targetByNode.get(index)||targetByNode.set(index,[]).get(index)).push(port)});const heap=[],push=item=>{heap.push(item);let i=heap.length-1;while(i){const parent=(i-1)>>1;if(heap[parent][0]<=item[0])break;heap[i]=heap[parent];i=parent}heap[i]=item},pop=()=>{const first=heap[0],last=heap.pop();if(heap.length&&last){let i=0;heap[0]=last;while(true){let child=i*2+1;if(child>=heap.length)break;if(child+1<heap.length&&heap[child+1][0]<heap[child][0])child++;if(heap[child][0]>=heap[i][0])break;[heap[i],heap[child]]=[heap[child],heap[i]];i=child}}return first},distance=new Map(),previous=new Map(),roots=new Map();for(const port of sourcePorts){const index=indexByKey.get(flowPointKey(port.lead));if(index===undefined)continue;const key=index+'|'+port.dir,cost=flowPortBias(port.side,from,to)+flowSegmentPenalty(port.point,port.lead,used);if(cost<(distance.get(key)??Infinity)){distance.set(key,cost);roots.set(key,port);push([cost,key,index,port.dir])}}let best=null;while(heap.length){const item=pop(),cost=item[0],key=item[1],index=item[2],dir=item[3];if(cost!==distance.get(key))continue;if(best&&cost>best.cost)break;for(const targetPort of targetByNode.get(index)||[]){const finalCost=cost+flowPortBias(targetPort.side,to,from)+flowSegmentPenalty(targetPort.lead,targetPort.point,used)+(dir===targetPort.dir?0:18);if(!best||finalCost<best.cost)best={cost:finalCost,key,targetPort}}for(const neighbor of neighbors[index]){const nextIndex=neighbor[0],length=neighbor[1],nextDir=neighbor[2],a=points[index],b=points[nextIndex],nextCost=cost+length+(dir===nextDir?0:20)+flowSegmentPenalty(a,b,used),nextKey=nextIndex+'|'+nextDir;if(nextCost<(distance.get(nextKey)??Infinity)){distance.set(nextKey,nextCost);previous.set(nextKey,key);roots.set(nextKey,roots.get(key));push([nextCost,nextKey,nextIndex,nextDir])}}}if(!best){const fallback=connectorPoints(from,to);return flowSimplify([fallback.start,[fallback.start[0],fallback.end[1]],fallback.end])}const grid=[];let key=best.key;while(key){grid.push(points[Number(key.split('|')[0])]);key=previous.get(key)}grid.reverse();const root=roots.get(best.key),result=[root.point,root.lead,...grid,best.targetPort.lead,best.targetPort.point];return flowSimplify(result)}
function flowSelectEdge(svg,edgeId){const wasSelected=Boolean(edgeId&&svg.querySelector('[data-edge-id="'+edgeId+'"].is-selected'));svg.querySelectorAll('[data-edge-id]').forEach(item=>item.classList.remove('is-selected'));if(!edgeId||wasSelected)return;svg.querySelectorAll('[data-edge-id="'+edgeId+'"]').forEach(item=>item.classList.add('is-selected'))}
function flowPath(svg,points,className,markerId,edgeId,labelValue){const pathData=points.map((point,index)=>(index?'L':'M')+point[0]+' '+point[1]).join(' '),path=document.createElementNS(svg.namespaceURI,'path'),hit=document.createElementNS(svg.namespaceURI,'path');path.setAttribute('d',pathData);path.setAttribute('class','edge '+className);path.setAttribute('marker-end','url(#'+markerId+')');path.setAttribute('data-edge-id',edgeId);hit.setAttribute('d',pathData);hit.setAttribute('class','edge-hit');hit.setAttribute('data-edge-id',edgeId);hit.setAttribute('tabindex','0');hit.setAttribute('role','button');hit.setAttribute('aria-label',labelValue?'选择连线：'+text(labelValue):'选择流程连线');const select=()=>flowSelectEdge(svg,edgeId);hit.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();select()}});svg.append(path,hit);return path}
function flowOverlap(a,b,padding=0){return a.x<b.x+b.w+padding&&a.x+a.w+padding>b.x&&a.y<b.y+b.h+padding&&a.y+a.h+padding>b.y}
function flowLabel(svg,points,value,boxes,labelBoxes,width,height,edgeId){if(!value)return;const lines=wrapTextLines(value,14),lineHeight=14,longest=Math.max(...lines.map(line=>[...line].reduce((sum,char)=>sum+(/[\\u0000-\\u00ff]/.test(char)?.55:1),0)),5),labelWidth=Math.min(176,Math.max(64,longest*11+18)),labelHeight=lines.length*lineHeight+9,segments=[];for(let i=1;i<points.length;i++){const a=points[i-1],b=points[i],length=Math.abs(a[0]-b[0])+Math.abs(a[1]-b[1]);segments.push({a,b,length,horizontal:a[1]===b[1]})}segments.sort((a,b)=>(b.horizontal-a.horizontal)*10000+b.length-a.length);let chosen=null;for(const segment of segments){for(const ratio of [.5,.34,.66])for(const offset of segment.horizontal?[-labelHeight/2-7,labelHeight/2+7]:[labelWidth/2+8,-labelWidth/2-8]){const centerX=segment.a[0]+(segment.b[0]-segment.a[0])*ratio+(segment.horizontal?0:offset),centerY=segment.a[1]+(segment.b[1]-segment.a[1])*ratio+(segment.horizontal?offset:0),candidate={x:centerX-labelWidth/2,y:centerY-labelHeight/2,w:labelWidth,h:labelHeight};if(candidate.x<4||candidate.y<4||candidate.x+candidate.w>width-4||candidate.y+candidate.h>height-4)continue;if(boxes.some(box=>flowOverlap(candidate,box,3))||labelBoxes.some(box=>flowOverlap(candidate,box,4)))continue;chosen={...candidate,centerX,centerY};break}if(chosen)break}if(!chosen){const segment=segments[0];if(!segment)return;const centerX=(segment.a[0]+segment.b[0])/2,centerY=(segment.a[1]+segment.b[1])/2-labelHeight/2-7;chosen={x:centerX-labelWidth/2,y:centerY-labelHeight/2,w:labelWidth,h:labelHeight,centerX,centerY}}labelBoxes.push(chosen);const group=document.createElementNS(svg.namespaceURI,'g');group.setAttribute('class','edge-label-group');group.setAttribute('data-edge-id',edgeId);group.append(svgRect(document,chosen.x,chosen.y,chosen.w,chosen.h,'edge-label-box',3));const label=document.createElementNS(svg.namespaceURI,'text');label.setAttribute('x',chosen.centerX);label.setAttribute('y',chosen.centerY-(lines.length-1)*lineHeight/2+4);label.setAttribute('class','edge-label');lines.forEach((line,index)=>{const span=document.createElementNS(svg.namespaceURI,'tspan');span.setAttribute('x',chosen.centerX);span.setAttribute('dy',index===0?0:lineHeight);span.textContent=line;label.append(span)});group.append(label);svg.append(group)}
function flowNode(svg,node,box,layout,title,subtitle,pageId){const group=document.createElementNS(svg.namespaceURI,'g'),className='node '+(node.origin==='design_inference'||node.origin==='design_required'?'inference ':'')+(node.kind==='exception'||node.kind==='rollback'?node.kind:'');group.append(svgRect(document,box.x,box.y,box.w,box.h,className.trim(),3),svgWrappedText(document,box.x+13,box.y+23,title,'node-title',Math.max(18,Math.floor((box.w-26)/13)),18));if(layout.subtitleLines.length)group.append(svgWrappedText(document,box.x+13,box.y+23+layout.titleLines.length*18+9,subtitle,'node-sub',Math.max(22,Math.floor((box.w-26)/11)),16));if(pageId){group.style.cursor='pointer';group.addEventListener('click',()=>goPage(pageId))}svg.append(group)}
function swimlaneV2(){const s=d.businessFlow?.swimlane||{},phases=array(s.phases),lanes=array(s.lanes),nodes=array(s.nodes),edges=array(s.edges);if(!phases.length||!lanes.length)return el('p','empty','业务泳道数据未生成');const laneWidth=170,columnWidth=350,headerHeight=56,nodeWidth=292,nodeGap=30,cellPadding=28,grouped={},layouts={};nodes.forEach(node=>{const key=node.lane+'|'+node.phase;(grouped[key]??=[]).push(node);layouts[node.id]=nodeLayout(node.title,node.subtitle,nodeWidth)});const laneHeights=lanes.map(lane=>Math.max(170,...phases.map(phase=>{const items=grouped[lane.id+'|'+phase.id]||[];return cellPadding*2+items.reduce((sum,item)=>sum+layouts[item.id].height,0)+Math.max(0,items.length-1)*nodeGap}))),totalHeight=headerHeight+laneHeights.reduce((sum,value)=>sum+value,0),totalWidth=laneWidth+columnWidth*phases.length,svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.classList.add('svg');svg.setAttribute('viewBox','0 0 '+totalWidth+' '+totalHeight);svg.setAttribute('width',totalWidth);svg.setAttribute('height',totalHeight);addArrowMarker(svg,'swim-arrow-v2');phases.forEach((phase,index)=>{svg.append(svgRect(document,laneWidth+index*columnWidth,0,columnWidth,headerHeight,'phase'));svg.append(svgText(document,laneWidth+index*columnWidth+columnWidth/2,35,phase.title,'mid hd'))});let yOffset=headerHeight;const laneY={};lanes.forEach((lane,laneIndex)=>{const laneHeight=laneHeights[laneIndex];laneY[lane.id]=yOffset;svg.append(svgRect(document,0,yOffset,laneWidth,laneHeight,'lane'));svg.append(svgText(document,laneWidth/2,yOffset+laneHeight/2,lane.title,'mid lane-text'));phases.forEach((phase,index)=>svg.append(svgRect(document,laneWidth+index*columnWidth,yOffset,columnWidth,laneHeight,'cell')));yOffset+=laneHeight});const boxes={};nodes.forEach(node=>{const phaseIndex=phases.findIndex(item=>item.id===node.phase),lane=lanes.find(item=>item.id===node.lane);if(phaseIndex<0||!lane)return;const peers=grouped[node.lane+'|'+node.phase]||[],peerIndex=peers.indexOf(node),previousHeight=peers.slice(0,peerIndex).reduce((sum,item)=>sum+layouts[item.id].height+nodeGap,0),x=laneWidth+phaseIndex*columnWidth+(columnWidth-nodeWidth)/2,y=laneY[lane.id]+cellPadding+previousHeight;boxes[node.id]={x,y,w:nodeWidth,h:layouts[node.id].height}});const used=[],labels=[];edges.forEach((edge,edgeIndex)=>{const from=boxes[edge.from],to=boxes[edge.to];if(!from||!to)return;const points=flowRoute(from,to,Object.values(boxes),used,totalWidth,totalHeight);for(let i=1;i<points.length;i++)used.push([points[i-1],points[i]]);const edgeId='swim-edge-'+edgeIndex;flowPath(svg,points,edge.kind||'main','swim-arrow-v2',edgeId,edge.label);labels.push([points,edge.label,edgeId])});nodes.forEach(node=>{const box=boxes[node.id];if(box)flowNode(svg,node,box,layouts[node.id],node.title,node.subtitle,node.pageId)});const labelBoxes=[];labels.forEach(item=>flowLabel(svg,item[0],item[1],Object.values(boxes),labelBoxes,totalWidth,totalHeight,item[2]));svg.addEventListener('click',event=>{const target=event.target.closest?.('[data-edge-id]');flowSelectEdge(svg,target?.getAttribute('data-edge-id')||'')});return svg}
function flowRanks(pages,edges){const pageIds=new Set(pages.map(page=>page.id)),outgoing=new Map(),indegree=new Map(),rank=new Map();pages.forEach(page=>{outgoing.set(page.id,[]);indegree.set(page.id,0);rank.set(page.id,0)});edges.forEach(edge=>{if(!pageIds.has(edge.from)||!pageIds.has(edge.to)||edge.from===edge.to)return;outgoing.get(edge.from).push(edge.to);indegree.set(edge.to,indegree.get(edge.to)+1)});const queue=pages.filter(page=>indegree.get(page.id)===0).map(page=>page.id),visited=new Set();while(queue.length){const id=queue.shift();visited.add(id);for(const next of outgoing.get(id)){rank.set(next,Math.max(rank.get(next),rank.get(id)+1));indegree.set(next,indegree.get(next)-1);if(indegree.get(next)===0)queue.push(next)}}pages.forEach((page,index)=>{if(!visited.has(page.id))rank.set(page.id,Math.max(rank.get(page.id),Math.floor(index/2)))});return rank}
function pageFlowV2(){const lanes=array(f.lanes),pages=array(f.pages),edges=array(f.edges);if(!pages.length)return el('p','empty','页面流程数据未生成');const laneWidth=158,cardWidth=300,columnGap=118,rowGap=28,lanePadding=28,rank=flowRanks(pages,edges),byLaneRank=new Map(),layouts={};pages.forEach(page=>{const titleLines=wrapTextLines(page.title,22),domainLines=wrapTextLines(page.businessDomain||page.lane,26),moduleLines=wrapTextLines(array(page.modules).join(' / '),26);layouts[page.id]={titleLines,domainLines,moduleLines,height:Math.max(112,20+titleLines.length*18+9+domainLines.length*16+(moduleLines.length?7+moduleLines.length*16:0)+16)};const key=page.lane+'|'+rank.get(page.id);(byLaneRank.get(key)||byLaneRank.set(key,[]).get(key)).push(page)});const maxRank=Math.max(0,...rank.values()),laneHeights=lanes.map(lane=>{let tallest=0;for(let column=0;column<=maxRank;column++){const items=byLaneRank.get(lane.id+'|'+column)||[],height=items.reduce((sum,page)=>sum+layouts[page.id].height,0)+Math.max(0,items.length-1)*rowGap;tallest=Math.max(tallest,height)}return Math.max(150,tallest+lanePadding*2)}),totalWidth=laneWidth+42+(maxRank+1)*(cardWidth+columnGap),totalHeight=laneHeights.reduce((sum,value)=>sum+value,0),svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.classList.add('svg');svg.setAttribute('viewBox','0 0 '+totalWidth+' '+totalHeight);svg.setAttribute('width',totalWidth);svg.setAttribute('height',totalHeight);addArrowMarker(svg,'page-arrow-v2');const boxes={},laneY={};let yOffset=0;lanes.forEach((lane,laneIndex)=>{const laneHeight=laneHeights[laneIndex];laneY[lane.id]=yOffset;svg.append(svgRect(document,0,yOffset,laneWidth,laneHeight,'lane'));svg.append(svgText(document,laneWidth/2,yOffset+laneHeight/2,lane.title,'mid lane-text'));yOffset+=laneHeight});lanes.forEach(lane=>{for(let column=0;column<=maxRank;column++){const items=byLaneRank.get(lane.id+'|'+column)||[],stackHeight=items.reduce((sum,page)=>sum+layouts[page.id].height,0)+Math.max(0,items.length-1)*rowGap;let y=laneY[lane.id]+Math.max(lanePadding,(laneHeights[lanes.findIndex(item=>item.id===lane.id)]-stackHeight)/2);items.forEach(page=>{const x=laneWidth+34+column*(cardWidth+columnGap);boxes[page.id]={x,y,w:cardWidth,h:layouts[page.id].height};y+=layouts[page.id].height+rowGap})}});const used=[],labels=[];edges.forEach((edge,edgeIndex)=>{const from=boxes[edge.from],to=boxes[edge.to];if(!from||!to)return;const points=flowRoute(from,to,Object.values(boxes),used,totalWidth,totalHeight);for(let i=1;i<points.length;i++)used.push([points[i-1],points[i]]);const edgeId='page-edge-'+edgeIndex;flowPath(svg,points,'main','page-arrow-v2',edgeId,edge.label);labels.push([points,edge.label,edgeId])});pages.forEach(page=>{const box=boxes[page.id],layout=layouts[page.id];if(!box)return;const group=document.createElementNS(svg.namespaceURI,'g');group.style.cursor='pointer';group.append(svgRect(document,box.x,box.y,box.w,box.h,'node '+(page.origin==='design_required'?'inference':''),3),svgWrappedText(document,box.x+13,box.y+24,page.title,'node-title',22,18));let textY=box.y+24+layout.titleLines.length*18+8;group.append(svgWrappedText(document,box.x+13,textY,page.businessDomain||page.lane,'node-sub',26,16));textY+=layout.domainLines.length*16+7;if(layout.moduleLines.length)group.append(svgWrappedText(document,box.x+13,textY,array(page.modules).join(' / '),'node-sub',26,16));group.addEventListener('click',()=>goPage(page.id));svg.append(group)});const labelBoxes=[];labels.forEach(item=>flowLabel(svg,item[0],item[1],Object.values(boxes),labelBoxes,totalWidth,totalHeight,item[2]));svg.addEventListener('click',event=>{const target=event.target.closest?.('[data-edge-id]');flowSelectEdge(svg,target?.getAttribute('data-edge-id')||'')});return svg}
const d=JSON.parse(document.getElementById('analysis-data').textContent),f=JSON.parse(document.getElementById('page-flow-schema').textContent),q=(s,r=document)=>r.querySelector(s),all=(s,r=document)=>[...r.querySelectorAll(s)];
const el=(tag,className,text)=>{const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=String(text);return node};
const array=value=>Array.isArray(value)?value:(value===undefined||value===null||value===''?[]:[value]);
const text=value=>{if(value===true)return '是';if(value===false)return '否';if(value===undefined||value===null||value==='')return '—';if(Array.isArray(value))return value.map(text).join('；');if(typeof value==='object')return value.title||value.name||value.text||value.description||value.question||JSON.stringify(value);return String(value)};
const sources=value=>{const box=el('span','sources');array(value).forEach(item=>box.append(el('span','source',item)));return box};
function table(headers,rows,options={}){const wrap=el('div','table-wrap table-complete '+(options.className||'')),tableNode=el('table'),colgroup=el('colgroup'),thead=el('thead'),headRow=el('tr');if(options.minWidth)tableNode.style.minWidth=options.minWidth+'px';array(options.widths).forEach(width=>{const col=el('col');col.style.width=width;colgroup.append(col)});if(colgroup.children.length)tableNode.append(colgroup);headers.forEach(header=>headRow.append(el('th','',header)));thead.append(headRow);tableNode.append(thead);const tbody=el('tbody');if(!rows.length){const row=el('tr'),cell=el('td','empty','暂无明确内容');cell.colSpan=headers.length;row.append(cell);tbody.append(row)}else rows.forEach((values,index)=>{const row=el('tr',options.rowClass?options.rowClass(values,index):'');values.forEach(value=>{const cell=el('td');if(value instanceof Node)cell.append(value);else cell.textContent=text(value);row.append(cell)});tbody.append(row)});tableNode.append(tbody);wrap.append(tableNode);return wrap}
function section(titleText){const node=el('section','section');node.append(el('h2','',titleText));return node}
function list(items,emptyText='暂无明确内容'){const ul=el('ul','list');const values=array(items);if(!values.length)ul.append(el('li','empty',emptyText));else values.forEach(item=>ul.append(el('li','',text(item))));return ul}
function svgRect(doc,x,y,w,h,className,rx=0){const node=doc.createElementNS('http://www.w3.org/2000/svg','rect');for(const [key,value] of Object.entries({x,y,width:w,height:h,rx,class:className}))node.setAttribute(key,value);return node}
function svgText(doc,x,y,value,className){const node=doc.createElementNS('http://www.w3.org/2000/svg','text');node.setAttribute('x',x);node.setAttribute('y',y);node.setAttribute('class',className);node.textContent=text(value);return node}
function wrapTextLines(value,maxChars){const raw=text(value).replace(/\\s+/g,' ').trim();if(!raw||raw==='—')return [];const lines=[];let current='';for(const character of raw){const weight=/[\\u0000-\\u00ff]/.test(character)?0.55:1;if(current.split('').reduce((sum,item)=>sum+(/[\\u0000-\\u00ff]/.test(item)?0.55:1),0)+weight>maxChars&&current){lines.push(current);current=character}else current+=character}if(current)lines.push(current);return lines}
function svgWrappedText(doc,x,y,value,className,maxChars,lineHeight){const node=doc.createElementNS('http://www.w3.org/2000/svg','text');node.setAttribute('x',x);node.setAttribute('y',y);node.setAttribute('class',className);wrapTextLines(value,maxChars).forEach((line,index)=>{const span=doc.createElementNS('http://www.w3.org/2000/svg','tspan');span.setAttribute('x',x);span.setAttribute('dy',index===0?0:lineHeight);span.textContent=line;node.append(span)});return node}
function nodeLayout(title,subtitle,width){const titleLines=wrapTextLines(title,Math.max(12,Math.floor((width-20)/13))),subtitleLines=wrapTextLines(subtitle,Math.max(14,Math.floor((width-20)/11)));return{titleLines,subtitleLines,height:Math.max(76,16+titleLines.length*18+(subtitleLines.length?8+subtitleLines.length*15:0)+12)}}
function addArrowMarker(svg,id,color='#2463b8'){const defs=document.createElementNS(svg.namespaceURI,'defs'),marker=document.createElementNS(svg.namespaceURI,'marker'),arrow=document.createElementNS(svg.namespaceURI,'path');marker.setAttribute('id',id);marker.setAttribute('markerWidth','9');marker.setAttribute('markerHeight','9');marker.setAttribute('refX','8');marker.setAttribute('refY','3.5');marker.setAttribute('orient','auto');marker.setAttribute('markerUnits','strokeWidth');arrow.setAttribute('d','M0,0L0,7L9,3.5z');arrow.setAttribute('fill',color);marker.append(arrow);defs.append(marker);svg.append(defs);return defs}
function connectorPoints(from,to){const fromCenter=[from.x+from.w/2,from.y+from.h/2],toCenter=[to.x+to.w/2,to.y+to.h/2],horizontal=Math.abs(toCenter[0]-fromCenter[0])>=Math.abs(toCenter[1]-fromCenter[1]);if(horizontal){const forward=toCenter[0]>=fromCenter[0];return{start:[forward?from.x+from.w:from.x,fromCenter[1]],end:[forward?to.x:to.x+to.w,toCenter[1]]}}const forward=toCenter[1]>=fromCenter[1];return{start:[fromCenter[0],forward?from.y+from.h:from.y],end:[toCenter[0],forward?to.y:to.y+to.h]}}
function edgeLabel(svg,x,y,value){if(!value)return;const label=text(value),width=Math.min(220,Math.max(56,label.length*11+16)),group=document.createElementNS(svg.namespaceURI,'g');group.append(svgRect(document,x-width/2,y-13,width,22,'edge-label-box',2),svgText(document,x,y+2,label,'edge-label'));svg.append(group)}
function typeBadge(value){const raw=asReviewType(value),label=raw==='risk'?'设计风险':raw==='protection'?'保护建议':'设计要点',className=raw==='risk'?'type-risk':raw==='protection'?'type-protection':'type-design';return el('span','type-badge '+className,label)}
function asReviewType(value){const raw=String(value||'').toLowerCase();if(raw.includes('risk')||raw.includes('风险'))return'risk';if(raw.includes('protection')||raw.includes('保护'))return'protection';return'design_point'}
function evidenceBadge(value){const raw=String(value||'').toLowerCase(),verified=['verified','found','designed','source-fact'].includes(raw),partial=raw==='partial',blocked=['blocked','inaccessible'].includes(raw),label=verified?'已核验':partial?'部分核验':blocked?'访问受阻':'未发现直接证据',className=verified?'status-ok':partial?'status-warn':blocked?'status-risk':'status-warn';return el('span','status '+className,label)}
function goPage(id){q('#tab-requirement-analysis').click();setTimeout(()=>{const button=q('[data-page-id="'+CSS.escape(id)+'"]');if(button){button.click();button.scrollIntoView({behavior:'smooth',block:'center'})}},0)}
function swimlane(){const s=d.businessFlow?.swimlane||{},phases=array(s.phases),lanes=array(s.lanes),nodes=array(s.nodes),edges=array(s.edges);if(!phases.length||!lanes.length)return el('p','empty','业务泳道数据未生成');const laneWidth=164,columnWidth=330,headerHeight=52,nodeWidth=286,nodeGap=16,grouped={},layouts={};nodes.forEach(node=>{const key=node.lane+'|'+node.phase;(grouped[key]??=[]).push(node);layouts[node.id]=nodeLayout(node.title,node.subtitle,nodeWidth)});const laneHeights=lanes.map(lane=>Math.max(150,...phases.map(phase=>{const items=grouped[lane.id+'|'+phase.id]||[];return 28+items.reduce((sum,item)=>sum+layouts[item.id].height,0)+Math.max(0,items.length-1)*nodeGap}))),totalHeight=headerHeight+laneHeights.reduce((sum,height)=>sum+height,0),totalWidth=laneWidth+columnWidth*phases.length,svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.classList.add('svg');svg.setAttribute('viewBox','0 0 '+totalWidth+' '+totalHeight);addArrowMarker(svg,'swim-arrow');phases.forEach((phase,index)=>{svg.append(svgRect(document,laneWidth+index*columnWidth,0,columnWidth,headerHeight,'phase'));svg.append(svgText(document,laneWidth+index*columnWidth+columnWidth/2,32,phase.title,'mid hd'))});let yOffset=headerHeight;const laneY={};lanes.forEach((lane,laneIndex)=>{const height=laneHeights[laneIndex];laneY[lane.id]=yOffset;svg.append(svgRect(document,0,yOffset,laneWidth,height,'lane'));svg.append(svgText(document,laneWidth/2,yOffset+height/2,lane.title,'mid lane-text'));phases.forEach((phase,index)=>svg.append(svgRect(document,laneWidth+index*columnWidth,yOffset,columnWidth,height,'cell')));yOffset+=height});const boxes={};nodes.forEach(node=>{const phaseIndex=phases.findIndex(item=>item.id===node.phase),lane=lanes.find(item=>item.id===node.lane);if(phaseIndex<0||!lane)return;const peers=grouped[node.lane+'|'+node.phase]||[],peerIndex=peers.indexOf(node),previousHeight=peers.slice(0,peerIndex).reduce((sum,item)=>sum+layouts[item.id].height+nodeGap,0),x=laneWidth+phaseIndex*columnWidth+(columnWidth-nodeWidth)/2,y=laneY[lane.id]+14+previousHeight;boxes[node.id]={x,y,w:nodeWidth,h:layouts[node.id].height}});edges.forEach(edge=>{const from=boxes[edge.from],to=boxes[edge.to];if(!from||!to)return;const points=connectorPoints(from,to),start=points.start,end=points.end,horizontal=Math.abs(end[0]-start[0])>=Math.abs(end[1]-start[1]),path=document.createElementNS(svg.namespaceURI,'path');let labelX,labelY;if(horizontal){const middle=(start[0]+end[0])/2;path.setAttribute('d','M'+start[0]+' '+start[1]+'H'+middle+'V'+end[1]+'H'+end[0]);labelX=middle;labelY=(start[1]+end[1])/2-14}else{const middle=(start[1]+end[1])/2;path.setAttribute('d','M'+start[0]+' '+start[1]+'V'+middle+'H'+end[0]+'V'+end[1]);labelX=(start[0]+end[0])/2;labelY=middle-10}path.setAttribute('class','edge '+(edge.kind||'main'));path.setAttribute('marker-end','url(#swim-arrow)');svg.append(path);edgeLabel(svg,labelX,labelY,edge.label)});nodes.forEach(node=>{const box=boxes[node.id];if(!box)return;const layout=layouts[node.id],group=document.createElementNS(svg.namespaceURI,'g'),className='node '+(node.origin==='design_inference'?'inference ':'')+(node.kind==='exception'||node.kind==='rollback'?node.kind:'');group.append(svgRect(document,box.x,box.y,box.w,box.h,className.trim(),3),svgWrappedText(document,box.x+12,box.y+22,node.title,'node-title',20,18));if(layout.subtitleLines.length)group.append(svgWrappedText(document,box.x+12,box.y+22+layout.titleLines.length*18+8,node.subtitle,'node-sub',25,15));if(node.pageId){group.style.cursor='pointer';group.addEventListener('click',()=>goPage(node.pageId))}svg.append(group)});return svg}
function pageFlow(){const lanes=array(f.lanes),pages=array(f.pages),edges=array(f.edges);if(!pages.length)return el('p','empty','页面流程数据未生成');const laneWidth=150,cardWidth=300,gap=76,rowGap=34,byLane={},layouts={};lanes.forEach(lane=>byLane[lane.id]=[]);pages.forEach(page=>{(byLane[page.lane]??=[]).push(page);const titleLines=wrapTextLines(page.title,22),domainLines=wrapTextLines(page.businessDomain||page.lane,26),moduleLines=wrapTextLines(array(page.modules).join(' / '),26);layouts[page.id]={titleLines,domainLines,moduleLines,height:Math.max(104,18+titleLines.length*18+8+domainLines.length*15+(moduleLines.length?6+moduleLines.length*15:0)+14)}});const maxCount=Math.max(1,...Object.values(byLane).map(items=>items.length)),laneHeights=lanes.map(lane=>Math.max(132,...(byLane[lane.id]||[]).map(page=>layouts[page.id].height+36))),totalWidth=laneWidth+30+maxCount*(cardWidth+gap),totalHeight=laneHeights.reduce((sum,height)=>sum+height,0),svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.classList.add('svg');svg.setAttribute('viewBox','0 0 '+totalWidth+' '+totalHeight);addArrowMarker(svg,'page-arrow');const boxes={};let yOffset=0;lanes.forEach((lane,laneIndex)=>{const laneHeight=laneHeights[laneIndex];svg.append(svgRect(document,0,yOffset,laneWidth,laneHeight,'lane'));svg.append(svgText(document,laneWidth/2,yOffset+laneHeight/2,lane.title,'mid lane-text'));(byLane[lane.id]||[]).forEach((page,index)=>{const x=laneWidth+30+index*(cardWidth+gap),y=yOffset+18;boxes[page.id]={x,y,w:cardWidth,h:layouts[page.id].height}});yOffset+=laneHeight});edges.forEach(edge=>{const from=boxes[edge.from],to=boxes[edge.to];if(!from||!to)return;const points=connectorPoints(from,to),start=points.start,end=points.end,horizontal=Math.abs(end[0]-start[0])>=Math.abs(end[1]-start[1]),path=document.createElementNS(svg.namespaceURI,'path');let labelX,labelY;if(horizontal){const middle=(start[0]+end[0])/2;path.setAttribute('d','M'+start[0]+' '+start[1]+'H'+middle+'V'+end[1]+'H'+end[0]);labelX=middle;labelY=(start[1]+end[1])/2-14}else{const middle=(start[1]+end[1])/2;path.setAttribute('d','M'+start[0]+' '+start[1]+'V'+middle+'H'+end[0]+'V'+end[1]);labelX=(start[0]+end[0])/2;labelY=middle-10}path.setAttribute('class','edge main');path.setAttribute('marker-end','url(#page-arrow)');svg.append(path);edgeLabel(svg,labelX,labelY,edge.label)});pages.forEach(page=>{const box=boxes[page.id];if(!box)return;const layout=layouts[page.id],group=document.createElementNS(svg.namespaceURI,'g');group.style.cursor='pointer';group.append(svgRect(document,box.x,box.y,box.w,box.h,'node '+(page.origin==='design_required'?'inference':''),3),svgWrappedText(document,box.x+12,box.y+23,page.title,'node-title',22,18));let textY=box.y+23+layout.titleLines.length*18+7;group.append(svgWrappedText(document,box.x+12,textY,page.businessDomain||page.lane,'node-sub',26,15));textY+=layout.domainLines.length*15+6;if(layout.moduleLines.length)group.append(svgWrappedText(document,box.x+12,textY,array(page.modules).join(' / '),'node-sub',26,15));group.addEventListener('click',()=>goPage(page.id));svg.append(group)});return svg}
function overview(){const node=section('需求总览'),metrics=el('div','metrics');[['涉及页面',d.pages.length],['原文条目',d.requirements.length],['未覆盖',array(d.coverage).filter(item=>item.status==='missing').length],['全局待确认',array(d.overview?.globalBlockers).length]].forEach(([label,value])=>{const card=el('div','metric');card.append(el('small','',label),el('strong','',value));metrics.append(card)});node.append(metrics,el('p','summary',d.overview?.summary||'—'));const facts=el('div','facts'),users=el('div','fact'),scenes=el('div','fact');users.append(el('b','','目标用户'),el('span','',text(d.overview?.targetUsers)));scenes.append(el('b','','使用场景'),el('span','',text(d.overview?.scenarios)));facts.append(users,scenes);node.append(facts);const blockers=el('div','blockers');blockers.id='overview-global-blockers';blockers.append(el('h3','','必须确认的全局问题'),table(['决策点','需要确认','影响范围','不确认后果','来源'],array(d.overview?.globalBlockers).map(item=>[item.title,item.decision||item.description,item.impact,item.consequence,sources(item.sources)]),{className:'blocker-table',minWidth:1180,widths:['16%','30%','18%','24%','12%']}));node.append(blockers);return node}
function requirementAnalysis(){const flow=section('业务流程'),swimWrap=el('div','diagram');swimWrap.id='business-swimlane';const swimTools=el('div','diagram-toolbar');swimTools.append(el('span','','阶段 × 参与方二维泳道；节点文字自动换行，副标题固定在节点底部区域，连线箭头指向下一节点'));swimWrap.append(swimTools,swimlane());const legend=el('div','legend');legend.innerHTML='<span><i></i>主路径</span><span><i class="branch"></i>分支</span><span><i class="risk"></i>异常/回退</span><span><i class="unknown"></i>待确认</span>';swimWrap.append(legend);flow.append(swimWrap,el('h3','','A–F 业务链路'),table(['层级','职责','关键功能点','数据来源','来源'],array(d.afLayers).map(item=>[item.layer,item.responsibility,item.keyFunctions,item.dataSources,sources(item.sources)]),{minWidth:1160,widths:['13%','23%','32%','20%','12%']}));const pages=section('页面总览'),flowWrap=el('div','diagram');flowWrap.id='page-flow';const pageTools=el('div','diagram-toolbar');pageTools.append(el('span','','页面层级与跳转关系；页面副标题固定分区，箭头表示跳转方向'));flowWrap.append(pageTools,pageFlow());pages.append(flowWrap);const unknown=array(f.unknownEdges);if(unknown.length)pages.append(el('h3','','未确定跳转'),table(['来源页面','入口/模块','未确定原因','来源'],unknown.map(item=>[item.from,item.fromModule,item.reason,sources(item.sources)]),{minWidth:920,widths:['20%','25%','40%','15%']}));return [flow,pages,pageDetails()]}
function pageDetails(){const node=section('逐页面详情'),tabs=el('div','subtabs'),content=el('div');function renderPage(page){content.replaceChildren();const article=el('article','detail'),header=el('div','detail-header'),heading=el('div');heading.append(el('h3','',page.name),el('p','meta',(page.module||'—')+' · '+(page.path||'—')+' · '+(page.pageType||'—')));header.append(heading,el('span','status '+(page.origin==='design_required'?'status-warn':'status-ok'),page.origin==='design_required'?'设计必需补充':'需求事实'));article.append(header);const context=el('section','detail-block');context.id='page-detail-context';context.append(el('h4','','页面上下文'));const grid=el('div','context-grid'),pre=el('div'),questions=el('div');pre.append(el('b','','前置条件'),list(page.preconditions));questions.append(el('b','','页面级待确认项'),list(array(page.openQuestions).map(item=>item.question||item.title||item.description),'暂无页面级待确认项'));grid.append(pre,questions);context.append(grid);const core=el('section','detail-block');core.id='page-detail-core';core.append(el('h4','','核心规格与交互'),table(['字段名','类型','必填','默认值','说明','限制'],array(page.fields).map(item=>[item.name,item.type,item.required,item.defaultValue,item.description,item.constraints]),{className:'field-table',minWidth:1180,widths:['16%','12%','8%','14%','28%','22%']}),el('h4','','交互规则'),list(page.interactionRules),el('h4','','操作流程'));const steps=el('ol','step-list');array(page.steps).forEach(item=>steps.append(el('li','',text(item))));if(!array(page.steps).length)steps.append(el('li','empty','暂无明确流程'));core.append(steps);const outcome=el('section','detail-block');outcome.id='page-detail-outcome-review';const reviewItems=[...array(page.designPoints),...array(page.designRisks)],reviewRows=reviewItems.map(item=>[typeBadge(item.type),item.fact||item.title||item.description,item.impact,item.advice,item.priority,sources(item.sources)]);outcome.append(el('h4','','结果与设计核对'),table(['成功','失败','异步','部分成功'],[[page.feedback?.success,page.feedback?.failure,page.feedback?.async,page.feedback?.partial]],{className:'outcome-feedback',minWidth:1040,widths:['25%','25%','25%','25%']}),table(['类型','事实/风险','影响','保护建议','优先级','来源'],reviewRows,{className:'outcome-review',minWidth:1280,widths:['10%','27%','20%','25%','8%','10%'],rowClass:(_,index)=>asReviewType(reviewItems[index]?.type)==='risk'?'row-risk':'row-design'}));article.append(context,core,outcome);content.append(article)}array(d.pages).forEach((page,index)=>{const button=el('button','subtab '+(page.origin==='design_required'?'inference':''),page.name);button.type='button';button.dataset.pageId=page.id;button.addEventListener('click',()=>{all('.subtab',tabs).forEach(item=>item.classList.remove('on'));button.classList.add('on');renderPage(page)});tabs.append(button);if(index===0){button.classList.add('on');renderPage(page)}});node.append(tabs,content);return node}
function designReview(){const node=section('跨页面设计约束');node.append(table(['约束','影响页面/操作','设计处理','来源'],array(d.designReview?.crossPageConstraints).map(item=>[item.rule||item.title||item,item.impactPages,item.design||item.advice,sources(item.sources)]),{minWidth:1120,widths:['27%','23%','36%','14%']}));const pageSection=section('全部页面设计要点与风险');array(d.designReview?.pages).forEach((group,index)=>{const page=array(d.pages).find(item=>item.id===group.pageId),items=array(group.items),card=el('details','review-card'),summary=el('summary'),title=el('span','',page?.name||group.pageId),count=el('small','',items.length+' 项'),body=el('div','review-card-body'),rows=items.map(item=>[typeBadge(item.type),item.fact||item.title||item.description,item.impact,item.advice,item.priority,sources(item.sources)]);if(index===0)card.open=true;summary.append(title,count);body.append(table(['类型','要点/风险','影响','保护建议','优先级','来源'],rows,{className:'review-table',minWidth:1280,widths:['10%','28%','20%','24%','8%','10%'],rowClass:(_,rowIndex)=>{const type=asReviewType(items[rowIndex]?.type);return type==='risk'?'row-risk':type==='protection'?'row-protection':'row-design'}}));card.append(summary,body);pageSection.append(card)});const terms=section('术语统一');terms.append(table(['界面术语','统一口径','不一致位置','来源'],array(d.designReview?.terminology).map(item=>[item.term||item.different||item.differentTerms||item.variants,item.definition||item.unified||item.unifiedTerm||item.preferred,item.inconsistencies||item.position||item.locations||item.usage,sources(item.sources||item.sourceRefs)]),{className:'terms-table',minWidth:1080,widths:['18%','40%','28%','14%']}));return [node,pageSection,terms]}
  function evidenceRefs(values,analysis){const box=el('div','source-list');array(values).forEach(id=>{const item=array(analysis.evidence).find(source=>source.id===id);if(!item)return;const link=el('a','source',id);link.href=item.url||'#';if(item.url){link.target='_blank';link.rel='noreferrer'}link.title=item.title||item.blocker||id;box.append(link)});return box}
  function competitorAnalysis(){const analysis=d.competitorAnalysis||d.competitors?.analysis||{},scope=section('功能范围');scope.append(el('p','summary',analysis.scope||'当前需求涉及的同类功能单元'),sources(analysis.scopeSourceRefs));const comparison=section('总体对比');comparison.append(el('p','summary','本品为移动云；竞品为阿里云、华为云、腾讯云。竞品事实均来自下方官方证据，设计启示与需求事实分列展示。'));const comparisonTable=table(['对比维度','移动云需求事实','阿里云','华为云','腾讯云','设计启示','证据'],array(analysis.matrix).map(item=>[item.dimension,item.mobileCloud,item.alibabaCloud,item.huaweiCloud,item.tencentCloud,item.designTakeaway,evidenceRefs(item.evidenceRefs,analysis)]),{className:'wide-table',minWidth:1760,widths:['13%','15%','15%','15%','15%','20%','7%']});comparison.append(comparisonTable);const cards=section('分功能证据卡');array(analysis.featureEvidence).forEach((feature,index)=>{const card=el('details','review-card');if(index===0)card.open=true;const summary=el('summary');summary.append(el('span','',feature.feature),el('small','',array(feature.products).length+' 个产品'));const body=el('div','review-card-body');body.append(table(['产品','入口与页面','结构、字段与校验','流程与状态反馈','风险保护','相关性','证据状态','证据'],array(feature.products).map(item=>[item.product,item.entryAndPage,item.structureAndValidation,item.flowAndFeedback,item.riskProtection,item.relevance,evidenceBadge(item.status),evidenceRefs(item.evidenceRefs,analysis)]),{className:'wide-table',minWidth:1520,widths:['8%','16%','19%','19%','15%','10%','7%','6%']}));card.append(summary,body);cards.append(card)});const evidence=section('证据与阻塞清单'),allEvidence=array(analysis.evidence),evidenceTable=table(['编号','产品','功能单元','证据状态','访问日期','官方来源 / 观察'],allEvidence.map(item=>{let last;if(item.url){const box=el('div'),link=el('a','evidence-link',item.title||item.url);link.href=item.url;link.target='_blank';link.rel='noreferrer';box.append(link);if(item.observation)box.append(el('div','diagram-note',item.observation));last=box}else last=item.blocker||item.gap||'当前未取得可靠官方证据';return[item.id,item.product,item.feature,evidenceBadge(item.evidenceStatus||item.status),item.accessedAt,last]}),{className:'evidence-table',minWidth:1180,widths:['7%','10%','14%','10%','10%','49%']});evidence.append(evidenceTable);return [scope,comparison,cards,evidence]}
const tabs=[['tab-requirement-analysis','需求解析',()=>[overview(),...requirementAnalysis()]],['tab-design-review','设计要点',designReview],['tab-competitor-analysis','竞品分析',competitorAnalysis]],tabsNode=q('#tabs'),app=q('#app');tabs.forEach(([id,label,render],index)=>{const button=el('button','tab',label);button.id=id;button.type='button';button.addEventListener('click',()=>{all('.tab',tabsNode).forEach(item=>item.classList.remove('on'));button.classList.add('on');app.replaceChildren(...render());window.scrollTo({top:0})});tabsNode.append(button);if(index===0){button.classList.add('on');app.replaceChildren(...render())}});q('#title').textContent=(d.meta?.title||d.meta?.product||'设计师需求解析')+' · '+(d.meta?.version||'v1.0');q('#subtitle').textContent='已索引 '+d.requirements.length+' 条原文 · '+d.pages.length+' 个页面 · 固定 Skill 合同输出';q('#back-top').addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
})();
</script></body></html>`
}
